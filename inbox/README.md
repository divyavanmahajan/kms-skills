# inbox/ — drop zone

Put anything you want ingested here (files, PDFs, a `.md` with a list of URLs,
raw notes), then run `/ingest`. The skill moves each item into the right
`sources/` subfolder with a dated name and compiles it into wiki pages.

This README stays put; everything else here is treated as pending material.
